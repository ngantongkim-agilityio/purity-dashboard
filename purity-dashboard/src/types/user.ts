export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type UserSession = {
  id: string;
  name: string;
  email: string;
  remember: string;
  expires: string;
};
